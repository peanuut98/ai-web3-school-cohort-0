# Week 2 Module C｜Agent Profile 草图：Agent Payment Auditor

## 1. 选择的 Agent / Workflow

我选择设计一个 **Agent Payment Auditor**，它是一个面向 AI Agent 付费任务场景的审计型工作流助手。

它的核心作用不是直接替用户付款，也不是自动控制钱包，而是帮助用户在 Agent 执行任务前后完成：

- 任务说明整理
- 预算与付款条件检查
- 调用记录追踪
- 交付结果核验
- 支付凭证与链上记录整理
- 失败原因分析与风险提示

这个 Agent 适合用于“AI Agent 帮人完成任务并收费”的场景，例如

- Agent 调用付费 API 获取数据
- Agent 生成报告后请求付款
- Agent 代用户完成一段工作流
- 用户需要确认 Agent 是否真的完成任务，是否值得付款

---

## 2. Agent Identity：它是谁

### 2.1 Agent 名称

**Agent Payment Auditor**

### 2.2 Agent 定位

Agent Payment Auditor 是一个用于审计 Agent 任务执行与付款过程的辅助型 Agent。

它不直接接触用户私钥，也不自动完成钱包签名，只负责帮助用户判断：

- 这个 Agent 是谁
- 它声称能做什么
- 它是否完成了承诺任务
- 它是否应该被支付
- 支付记录是否可验证
- 失败时责任应该如何划分

### 2.3 维护方

该 Agent 可以由以下主体维护：

- AI × Web3 应用团队
- Agent Marketplace 平台
- 企业内部自动化团队
- 开源社区维护者

维护方需要负责：

- 更新 Agent Profile
- 维护调用接口
- 记录版本变更
- 修复错误
- 公开能力边界
- 处理用户反馈和争议

---

## 3. Capability：它能做什么

Agent Payment Auditor 的能力可以拆成六类。

### 3.1 任务理解能力

它可以读取用户输入的任务需求，并整理成结构化任务说明。

示例：

```text
用户目标：生成一份市场分析报告
任务范围：收集公开信息、总结趋势、输出 Markdown 报告
预算上限：5 USDC
交付标准：不少于 800 字，包含来源说明
人工确认点：付款前必须由用户确认
```

### 3.2 Agent Profile 检查能力

它可以检查另一个 Agent 的基本资料，包括：

- Agent 名称
- 维护方
- 能力声明
- 输入格式
- 输出格式
- 收费方式
- 失败处理规则
- 历史执行记录
- 可验证的交付凭证

检查结果示例：

```text
Agent 名称：Research Agent
维护方：Example Team
能力声明：公开信息检索、内容总结、报告生成
输入格式：自然语言任务描述
输出格式：Markdown 报告
收费方式：按次收费
风险判断：中等风险
```

### 3.3 支付条件检查能力

它可以帮助用户判断付款条件是否清晰，包括：

- 是否有明确报价
- 是否有预算上限
- 是否写明什么情况才付款
- 是否支持失败退款
- 是否需要分阶段付款
- 是否有链上交易记录或收据

判断示例：

```text
报价：3 USDC
用户预算上限：5 USDC
是否超出预算：否
是否说明交付标准：是
是否说明失败退款：否
风险等级：中等
```

### 3.4 执行结果核验能力

当任务完成后，它可以对交付结果进行检查，包括：

- 是否完成用户最初要求
- 是否缺少关键内容
- 是否明显复制粘贴
- 是否输出格式正确
- 是否存在高风险错误
- 是否达到付款条件

核验示例：

```text
任务要求：生成不少于 800 字的市场分析报告
实际结果：约 1200 字，包含结构化标题和总结
是否满足字数要求：是
是否包含来源说明：部分包含
是否建议付款：可以付款，但建议人工复核来源
```

### 3.5 记录整理能力

它可以生成一份审计记录。

示例：

```text
任务 ID：task_001
调用 Agent：Research Agent
用户预算：5 USDC
实际报价：3 USDC
执行状态：已完成
人工确认：已确认
链上交易哈希：0x...
结果验证：基本通过
风险备注：部分数据来源仍需人工复核
```

### 3.6 风险提示能力

它可以提醒用户注意以下风险：

- 不要提交私钥、助记词、API Key
- 不要让 Agent 自动签名
- 不要在未验证结果前付款
- 不要授权无限额度
- 不要相信没有交付记录的 Agent
- 不要在金额、网络、地址不清楚时签名

风险提示示例：

```text
风险提示：
当前任务报价没有超过预算，但 Agent Profile 中没有明确退款机制。
建议用户在付款前再次确认收款地址、支付网络和实际签名金额。
```

---

## 4. 输入与输出

### 4.1 输入

Agent Payment Auditor 的输入包括：

| 输入类型 | 示例 |
|---|---|
| 用户任务描述 | “帮我检查这个 Agent 是否完成了报告任务” |
| Agent Profile | Agent 名称、能力、维护方、接口说明 |
| 任务报价 | 3 USDC / 每次调用 |
| 交付结果 | Markdown 报告、API 返回结果、文件链接 |
| 支付记录 | 钱包交易哈希、发票、收据 |
| 用户确认状态 | 是否人工确认付款 |

### 4.2 输出

它的输出是一份结构化审计结论。

示例：

```markdown
## Agent Payment Audit Result

任务状态：基本完成  
付款建议：可以付款，但建议人工复核数据来源  
风险等级：中低  

### 通过项

- Agent 输出了完整报告
- 报告结构符合用户要求
- 支付金额没有超过预算
- 有交易哈希可追踪

### 风险项

- 部分数据来源没有原始链接
- Agent Profile 中没有明确退款机制
- 维护方身份仍不够清晰

### 建议

可以进行付款，但用户应在钱包签名前再次确认金额、收款地址和交易网络。
```

---

## 5. 如何被调用

这个 Agent 可以通过三种方式被调用。

### 5.1 Web 前端调用

用户在网页中上传任务说明、Agent Profile 和交付结果，然后点击“开始审计”。

适合普通用户使用。

调用流程：

```text
用户输入任务信息
↓
上传 Agent Profile 和交付结果
↓
Agent Payment Auditor 进行检查
↓
输出审计结论
↓
用户决定是否付款
```

### 5.2 API 调用

其他 Agent 或平台可以通过 API 调用它。

示例：

```json
{
  "task_id": "task_001",
  "agent_profile": {
    "name": "Research Agent",
    "maintainer": "Example Team",
    "capability": ["search", "summarize", "generate_report"]
  },
  "payment": {
    "price": "3 USDC",
    "budget_limit": "5 USDC"
  },
  "deliverable": "report.md"
}
```

适合 Agent Marketplace 或自动化平台。

### 5.3 MCP 工具调用

如果它作为 MCP 工具存在，可以被 Claude、Cursor 或其他 Agent 系统调用，用来检查任务上下文和交付结果。

适合本地开发环境或 Agent 工具链。

示例：

```text
Claude / Agent
↓
通过 MCP 调用 Agent Payment Auditor
↓
读取任务说明、交付文件和支付记录
↓
生成审计报告
```

---

## 6. 如何收费

这个 Agent 本身可以采用轻量收费方式。

### 6.1 按次收费

每次审计收取固定费用。

示例：

```text
0.1 USDC / 次
```

适合低成本任务。

### 6.2 按任务金额比例收费

按照被审计任务金额的一定比例收费。

示例：

```text
审计费 = 任务金额的 1%
```

适合高价值任务。

### 6.3 免费基础版 + 高级版

基础检查免费，高级功能收费。

| 功能 | 是否收费 |
|---|---|
| 基础 Agent Profile 检查 | 免费 |
| 任务完成度检查 | 免费 |
| 链上支付记录整理 | 收费 |
| 多 Agent 协作审计 | 收费 |
| 企业级日志导出 | 收费 |

---

## 7. 如何被验证

Agent Payment Auditor 的可信度不能只靠名字，而要靠可验证记录。

### 7.1 Profile 验证

需要公开以下内容：

- Agent 名称
- 维护方
- 版本号
- 能力范围
- 不支持的事项
- 收费规则
- 失败处理规则

### 7.2 执行记录验证

每一次审计任务都应该有记录。

示例：

```text
task_id
timestamp
input_hash
output_hash
payment_status
review_result
```

这些记录可以证明某一次审计确实发生过。

### 7.3 链上记录验证

如果涉及付款，可以通过区块浏览器验证：

- 交易哈希
- 付款地址
- 收款地址
- 金额
- 网络
- 时间
- 状态

### 7.4 人工确认验证

最终付款必须由用户在钱包中人工确认。

Agent 只能给建议，不能替用户签名、授权或转账。

---

## 8. 协作对象

这个 Agent 不单独工作，它需要和多个对象协作。

| 协作对象 | 作用 |
|---|---|
| 用户 | 提供任务需求，最终确认是否付款 |
| 被审计 Agent | 提供任务执行结果 |
| 钱包 | 完成签名、授权、付款 |
| 区块浏览器 | 验证链上交易记录 |
| Agent Marketplace | 提供 Agent Profile 和历史评价 |
| Payment Layer | 处理报价、结算和收据 |
| MCP 工具 | 提供上下文和工具调用接口 |

---

## 9. 失败点与处理方式

### 9.1 Agent Profile 不完整

问题：

```text
Agent 没有说明维护方、收费方式或失败责任。
```

处理方式：

```text
标记为高风险，不建议直接付款。
```

### 9.2 任务边界不清楚

问题：

```text
用户说“帮我做一个报告”，但没有说明字数、格式、数据来源和交付标准。
```

处理方式：

```text
要求用户补充任务标准，再判断是否完成。
```

### 9.3 输出结果质量不足

问题：

```text
Agent 虽然给出了结果，但内容过短、错误明显或不符合要求。
```

处理方式：

```text
建议拒绝付款或要求返工。
```

### 9.4 支付金额异常

问题：

```text
Agent 报价为 3 USDC，但钱包签名时显示 30 USDC。
```

处理方式：

```text
立即提醒用户停止签名，并检查收款地址、网络和金额。
```

### 9.5 链上记录无法验证

问题：

```text
用户声称已经付款，但区块浏览器查不到交易。
```

处理方式：

```text
检查网络是否正确，确认交易哈希是否有效，避免重复付款。
```

### 9.6 Agent 执行失败

问题：

```text
API 超时、任务中断、返回空结果。
```

处理方式：

```text
记录失败原因，根据规则判断是否退款、重试或部分付款。
```

---

## 10. 安全边界

Agent Payment Auditor 必须遵守以下限制：

- 不读取用户私钥
- 不保存助记词
- 不自动发起转账
- 不绕过用户钱包确认
- 不替用户做最终投资或付款决策
- 不承诺结果绝对正确
- 不处理真实敏感账户信息

它只能辅助判断，不能替代用户确认。

---

## 11. 加分项：MCP 与 A2A 对比

### 11.1 MCP 适合解决什么问题

MCP 更适合解决 **Agent 与工具之间的连接问题**。

可以理解为：

```text
Agent 如何调用外部工具、读取上下文、访问文件、使用 API。
```

例如：

- Claude 调用本地文件系统
- Agent 调用数据库
- Agent 调用搜索工具
- Agent 调用支付记录查询工具
- Agent 调用 GitHub、Notion、浏览器等工具

在本项目中，MCP 可以用于：

```text
Agent Payment Auditor 调用任务文件、读取交付结果、查询交易记录、生成审计报告。
```

它关注的是：

- 工具接口
- 上下文传递
- 调用权限
- 输入输出格式

### 11.2 A2A 适合解决什么问题

A2A 更适合解决 **Agent 与 Agent 之间的协作问题**。

可以理解为：

```text
一个 Agent 如何发现、请求、调用、协作另一个 Agent。
```

例如：

- Research Agent 负责收集资料
- Writing Agent 负责生成报告
- Payment Auditor 负责检查交付结果
- Settlement Agent 负责整理付款记录

在本项目中，A2A 可以用于：

```text
当 Research Agent 完成任务后，把交付结果发送给 Agent Payment Auditor，由 Auditor 判断是否达到付款条件。
```

它关注的是：

- Agent 身份
- Agent 能力声明
- Agent 之间的通信
- 任务转交
- 协作状态

### 11.3 MCP 与 A2A 的区别

| 对比维度 | MCP | A2A |
|---|---|---|
| 主要对象 | Agent 与工具 | Agent 与 Agent |
| 核心问题 | 如何调用工具 | 如何协作任务 |
| 典型场景 | 调 API、读文件、查数据库 | 多 Agent 分工完成任务 |
| 关注重点 | 工具接口和上下文 | 身份、能力和协作协议 |
| 在本项目中的作用 | 查询交易记录、读取交付文件 | 接收其他 Agent 的任务结果并审计 |

简单来说：

```text
MCP 解决“Agent 怎么用工具”。
A2A 解决“Agent 怎么和其他 Agent 合作”。
```

---

## 12. 结论

Agent Payment Auditor 的核心价值是让 AI Agent 的任务执行和付款过程更加透明、可验证、可追责。

它不是一个自动付款工具，而是一个审计型辅助 Agent。它帮助用户在付款前看清楚：

- 对方 Agent 是谁
- 它承诺做什么
- 它是否真的完成
- 金额是否合理
- 付款是否可验证
- 失败后如何处理

在 AI × Web3 场景中，Agent 不仅需要能执行任务，还需要有清晰的 identity、capability、reputation 和 payment record。

只有当任务、身份、能力、交付和支付都能被验证时，Agent 协作网络才有可能真正建立信任。

---

## 13. 敏感信息声明

本作业不包含任何：

- 私钥
- 助记词
- API Key
- token
- .env 文件
- 真实资金账户信息
- 真实钱包敏感授权信息

所有地址、金额和任务 ID 均为示例。
